# Frontend Deployment — serves SPA, calls backend directly
# Usage: docker compose up -d --build

version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8002}:80"
    env_file:
      - .env   # <-- inject .env variables at build/runtime